A simple 32 bit(for now) kernel that will presumably be used for The Holy IsOs.
I dont recommend this project for daily usage as it is an extremely ameteur project made for fun and learning.

Includes:
a fully working print function and a semi-broken getch function

Dependencies:
sudo apt install gcc
sudo apt install qemu-system-i386
sudo apt install nasm

Building:
sh build.sh
